import 'package:flutter/material.dart';

class VisaVaccines extends StatelessWidget {
  const VisaVaccines({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
